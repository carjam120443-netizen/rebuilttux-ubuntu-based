# RebuiltTux build configuration
# Ubuntu 24.04 LTS uses the codename "noble" with debootstrap.
UBUNTU_RELEASE=noble
UBUNTU_VERSION=24.04
ARCH=amd64
DISTRO_NAME=RebuiltTux
DISTRO_ID=rebuilttux
DISTRO_VERSION=0.1
DESKTOP=xfce
